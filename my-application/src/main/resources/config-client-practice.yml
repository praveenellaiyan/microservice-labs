eureka:
  instance:
    hostname: localhost
  server:
    port: 8765
    location: http://${eureka.instance.hostname}:${eureka.server.port}/eureka

movies:
  client: 'api-gateway'
  all: '/movies/all'